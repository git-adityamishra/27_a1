{
  "PropertyAmenities": {
    "_id": "MongoDB ObjectId",
    "property_id": "Integer (MUST exactly match an integer property_id in Postgres)",
    "house_rules": [
      "Array of Strings (e.g., 'No smoking', 'No pets')"
    ],
    "accessibility_features": [
      "Array of Strings (e.g., 'Elevator', 'Step-free access')"
    ]
  },
  "PropertyReviews": {
    "_id": "MongoDB ObjectId",
    "property_id": "Integer (MUST exactly match an integer property_id in Postgres)",
    "guest_id": "Integer (MUST exactly match an integer guest_id in Postgres)",
    "rating": "Double (Number from 1.0 to 5.0)",
    "review_text": "String (A long paragraph of text)",
    "location_tags": [
      "Array of Strings (e.g., 'Beachfront', 'Downtown')"
    ],
    "timestamp": "ISODate (Time the review was written)"
  },
  "SearchSessions": {
    "_id": "MongoDB ObjectId",
    "session_id": "String (UUID - this one can stay a UUID because it is unique to Mongo)",
    "user_device": "String (e.g., 'Mobile', 'Desktop')",
    "location": {
      "type": "String (Must strictly be the word 'Point')",
      "coordinates": [
        "Array of Doubles: [Longitude, Latitude] - MUST be in this specific order for MongoDB 2dsphere!"
      ]
    },
    "created_at": "ISODate (Used by the TTL index to auto-delete after 2 hours)"
  }
}

